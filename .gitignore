target/
.DS_Store
node_modules/
dist/
build/
*.log
.env
.env.local
*.swp
*.swo
*~
.idea/
.vscode/
*.iml
.anchor/
dist/

# Build output: the exported deck, the site zip, and the terminal recording
# are all regenerated by `npm run export-deck`, `zip`, and asciinema.
*.zip
demo.cast
demo.gif

# Private keys for the persistent demo merchants. Devnet play money only, but
# a committed secret key is a committed secret key. The public addresses live
# in frontend/merchants.json, which is safe to commit and is what the site reads.
.demo-wallets/
demo-wallet.json
